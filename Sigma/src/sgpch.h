#pragma once

// RenderApi
#include <glad/glad.h>
#include <GLFW/glfw3.h>

#include <memory>
#include <iostream>

#include <string>
#include <vector>
#include <functional>

/*
// Platform
#ifdef _WIN32
    #ifdef _WIN64
        #define SIGMA_WIN64
    #elif
        #define SIGMA_WIN32
    #endif
#elif __unix__
    throw "Linux is currently not supported"
#endif

// Windows
#ifdef _WIN32
    #include <Windows.h>
#endif
*/