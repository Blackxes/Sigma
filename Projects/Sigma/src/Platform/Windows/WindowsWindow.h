/**
 * @File This file is part of the Sigma game engine
 */

#pragma once

//#include "Core/Base.h"
//#include "Core/RenderWindow.h"

//class RenderWindow;

namespace Sigma
{
    /*
    class SIGMA_API WindowsWindow : public RenderWindow
    {
    public:
        WindowsWindow(const RenderWindowCreationOptions& creationOptions) : RenderWindow(creationOptions) {}

        bool Init() override;
        bool Shutdown() override;
        bool InitializeOpenGL();
        bool Create();
        bool CloseWindow() override;
        bool CreateConsole() override;

    private:
        GLFWwindow* m_windowHandle = nullptr;
        bool m_initialized = false;
        bool m_isOpen = false;

        void* GetTypeExplicitRenderWindowHandle() override
        {
            return (void*) this->m_windowHandle;
        }
    };
    */
}